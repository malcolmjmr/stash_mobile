import 'package:flutter/material.dart';

class WorkspaceModal extends StatelessWidget {
  const WorkspaceModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}