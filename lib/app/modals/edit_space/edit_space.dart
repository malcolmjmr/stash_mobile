import 'package:flutter/material.dart';

class EditSpaceModal extends StatelessWidget {
  const EditSpaceModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}