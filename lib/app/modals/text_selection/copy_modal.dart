import 'package:flutter/material.dart';

class CopyModal extends StatelessWidget {
  const CopyModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    /*
      search
        
      copy to clipboard
      
      list of notes
      - 

    */
    return Container();
  }
}