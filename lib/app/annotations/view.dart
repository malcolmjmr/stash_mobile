import 'package:flutter/material.dart';
import 'package:stashmobile/models/content/content.dart';

class AnnotationsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class AnnotationThumbnailView extends StatelessWidget {
  final Content content;
  AnnotationThumbnailView(this.content);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
