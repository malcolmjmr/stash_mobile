import 'package:flutter/material.dart';

class DeleteSpaceDialog extends StatelessWidget {
  const DeleteSpaceDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Container(),
      ),
    );
  }
}